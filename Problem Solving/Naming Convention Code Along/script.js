const currentArticle= "News in the world today";
const currentArticle = "Lorem ipsum";

const hasPassedTest = true; //start with has or is for boolen
const amountOfWords = 957; //amount indicates number
const headlineMaxLength = 15;

if(headline.length > headlineMaxLength){
    headline = headline.substring(0,headlineMaxLength - 3) + "...";
}

console.log("Nology Post 2021");
console.log(`Headline: ${headline}`); //using ` for text allows easy variabel isertion with ${}
console.log(article);


function GetPost(){ //meaningful function name start with get
    //get post
}